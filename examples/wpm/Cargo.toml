[package]
name = "wpm-ssh"
version = "0.2.0"
edition = "2024"
publish = false

[dependencies]
sshui = { path = "../../", features = ["keyring"] }

tokio = { version = "1", features = ["full"] }
anyhow = "1"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
