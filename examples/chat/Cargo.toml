[package]
name = "chat-ssh"
version = "0.2.0"
edition = "2024"
publish = false

[dependencies]
sshui = { path = "../../", features = ["keyring"] }

tokio = { version = "1", features = ["full"] }
anyhow = "1"