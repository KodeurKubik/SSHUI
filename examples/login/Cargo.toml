[package]
name = "login-ssh"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
sshui = { path = "../../", features = ["keyring"] }

tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
anyhow = "1"